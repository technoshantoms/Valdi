// Opaque instance that is used by the framework to trigger monitoring performance while the user is scrolling.
//
// @NativeInterface({ marshallAsUntyped: true, ios: 'SCValdiIScrollPerfLoggerBridge', android: 'com.snap.foundation.IScrollPerfLoggerBridge' })
export interface IScrollPerfLoggerBridge {}
