diff --git a/BUILD.bazel b/BUILD.bazel
index a03a661..f8fc79e 100755
--- a/BUILD.bazel
+++ b/BUILD.bazel
@@ -47,6 +47,22 @@ config_setting(
     ],
 )

+config_setting(
+    name = "ios_x64",
+    constraint_values = [
+        "@platforms//cpu:x86_64",
+        "@platforms//os:ios",
+    ],
+)
+
+config_setting(
+    name = "android_x64",
+    constraint_values = [
+        "@platforms//cpu:x86_64",
+        "@platforms//os:android",
+    ],
+)
+
 config_setting(
     name = "mac_arm64",
     constraint_values = [
@@ -164,6 +180,12 @@ ZLIB_COPTS = [
     ":mac_x64": [
         "-mpclmul",
     ],
+    ":ios_x64": [
+        "-mpclmul",
+    ],
+    ":android_x64": [
+        "-mpclmul",
+    ],
     ":windows_x64": [
         "-mssse3",
         "-msse4.2",
