#import <UIKit/UIKit.h>

/**
 * Launch a UIKit application backed by Valdi.
 * It will the given root component path as the root.
 */
int SCValdiAppMain(int argc, char* argv[], NSString* rootComponentPath);
