load("@valdi//bzl/valdi:valdi_module.bzl", "valdi_module")

cc_library(
    name = "{{MODULE_NAME}}_cpp",
    srcs = glob(["cpp/**/*.cpp"]),
    hdrs = glob(["cpp/**/*.hpp"]),
    includes = [],
    deps = ["@valdi//valdi_core"],
    alwayslink = 1,
)

valdi_module(
    name = "{{MODULE_NAME}}",
    srcs = glob([
        "src/**/*.ts",
        "src/**/*.tsx",
    ]),
    native_deps = [":{{MODULE_NAME}}_cpp"],
    visibility = ["//visibility:public"],
    deps = [],
)
